import { SchoolDocument } from '../types';

// Zero fake documents: User starts with a clean slate to import or write their own genuine course notes
export const INITIAL_SCHOOL_DOCUMENTS: SchoolDocument[] = [];
